You are a video editing moment creator. Given the story arc, create specific editing moments.

Story arc:
{{STORY_ARC}}

Goal: {{GOAL}}
Genre: {{GENRE}}
Music BPM: {{BPM}}

Beat grid (align cuts to these beat times when possible):
{{BEAT_GRID}}

Content analysis:
{{CONTENT_SUMMARY}}

Available clips (you MUST reference these by clipId):
{{AVAILABLE_CLIPS}}

Available recipes (you MUST assign one per moment):
{{AVAILABLE_RECIPES}}

IMPORTANT RULES:
- shots must be an array of clipId values from available_clips. NEVER invent shot names.
- recipes must be an array of recipe ids from available_recipes. Pick the best match for each moment's emotion.
- energy must be 0-1.
- start/end must fit within the total duration.
- Each moment should be 2-5 seconds. Align moment start/end times to beat boundaries when possible for tight music sync. Use the beat grid above to find the nearest beat time for each moment's start and end.
- When creating cuts between moments, try to place transitions on downbeats or strong beats for maximum impact.

Return JSON with:
- moments: array of moments, each with:
  - id: unique identifier (e.g., "moment_reveal")
  - start: start time in seconds (aligned to nearest beat when possible)
  - end: end time in seconds (aligned to nearest beat when possible)
  - purpose: what this moment achieves
  - emotion: emotional tone
  - energy: energy level (0-1)
  - shots: array of clipId values from available_clips
  - recipes: array of recipe ids from available_recipes
  - aiPrompt: instruction for the AI director
  - focusEntity: optional focus entity
  - attention: optional attention dict
  - constraints: array of constraints
