{
  "name": "@monet/worker-node",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@monet/edl": "workspace:",
    "@monet/edl-enhancers": "workspace:",
    "@monet/job-contracts": "workspace:",
    "@monet/render-adapters": "workspace:",
    "bullmq": "^5.78.0",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
