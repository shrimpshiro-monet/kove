import { CompositingItem } from "../types";

export const DreamyOverlay: CompositingItem = {
  id: "dreamy-overlay",
  blendMode: "screen",
  opacity: 0.6,
  aiRationale: "Use for flashbacks or ethereal, soft-focus sequences."
};
